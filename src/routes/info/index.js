import {h, Component} from 'preact';
import {style} from './style';

class Info extends Component {
    state = {  }

    render() { 
        return (
            <main>
            </main>
        );
    }
}
 
export default Info;